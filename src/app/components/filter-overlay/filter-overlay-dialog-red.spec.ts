import { FilterOverlayDialogRef } from './filter-overlay-dialog-ref';

describe('FilterOverlayDialogRef', () => {
  it('should create an instance', () => {
    expect(new FilterOverlayDialogRef()).toBeTruthy();
  });
});
